version: 0.0.3
name: bria_background_removal
displayName: "%display-name%"
description: "%description%"
repository: https://github.com/oomol-flows/bria-removebg.git
releaseNotes: "fix: add params type"
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
icon: ./icon.png
dependencies:
  upload-to-cloud: 0.0.5
  downloader: 0.1.1
